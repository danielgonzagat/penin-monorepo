# === ET★ AutoEvolve (agressivo) ===
LLAMA_URL=http://127.0.0.1:8080/v1/chat/completions
LLAMA_MODEL=qwen2.5-7b-instruct
LLAMA_KEY=DANIEL

# Iteração quase contínua
ITER_SLEEP=1

# Meta de melhoria mínima do score para aceitar nova iteração
IMPROVE_DELTA=0.01

# Tolerância de latência (até 25% pior sem travar o loop)
MAX_LAT_WORSE=1.25

# Exploração ampla de parâmetros por rodada
EXPLORE_TEMP=0.3,0.6,0.9
EXPLORE_MAXTOK=256,512,768

# Paralelismo de variações por rodada (suba/diminua conforme CPU)
PARALLEL=6

# Caminhos de canários (públicos e ocultos)
CANARIOS=/opt/et/canarios.json
CANARIOS_HIDDEN=/opt/et/canarios_hidden.json

# Logs / histórico
HISTORY=/var/log/et/autoevolve_history.jsonl
LOGFILE=/var/log/et/autoevolve.log
